# Odd or Even Checker 🔢

A simple Python project with **both CLI and GUI** to check if a number is odd or even.  
Perfect for beginners and as a showcase project on GitHub.

---

## 🚀 Features
- **CLI** (Command Line Interface) using `argparse`
- **GUI** using `tkinter` (no external dependencies)
- Tested with **pytest**

---

## 📂 Project Structure
odd-or-even-checker/
├─ odd_even_checker/
│ ├─ init.py
│ ├─ core.py
│ ├─ cli.py
│ └─ gui.py
├─ tests/
│ └─ test_core.py
├─ main.py
├─ README.md
├─ requirements.txt
└─ .gitignore

yaml
Copy
Edit

---

## ⚙️ Installation & Usage

1. Clone repo & create venv:
```bash
git clone https://github.com/your-username/odd-or-even-checker.git
cd odd-or-even-checker
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows
Install requirements:

bash
Copy
Edit
pip install -r requirements.txt
Run tests:

bash
Copy
Edit
pytest -q
Run the app:

bash
Copy
Edit
python main.py
Choose CLI or GUI mode from the menu.

📸 Screenshots
(Add screenshot of GUI here)

📜 License
This project is open-source. You may add a license (MIT recommended).
